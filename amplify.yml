version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install --prefer-offline --no-audit --no-fund --loglevel error
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - '**/*'
