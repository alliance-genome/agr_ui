version: 1
frontend:
  phases:
    preBuild:
      commands:
        - mkdir dist
        - nvm use
        - npm install
    build:
      commands:
        - NODE_ENV=$BUILD_ENV npm run build
  artifacts:
    baseDirectory: dist/apps/main-app
    files:
      - '**/*'
